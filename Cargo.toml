[package]
name = "linguaspark"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2"

[build-dependencies]
minreq = { version = "2", features = ["https-native"] }

[target.'cfg(any(target_arch = "x86_64", target_arch = "x86"))'.build-dependencies]
intel-mkl-tool = "0.8"
